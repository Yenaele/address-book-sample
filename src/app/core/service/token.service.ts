import {Injectable} from '@angular/core';

@Injectable()
export class TokenService {

  /**
   * this is a service to get the current login user
   * token for backend validate.
   */

  public getToken() {
    return '';
  }

}
